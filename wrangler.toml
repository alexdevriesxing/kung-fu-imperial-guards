name = "kung-fu-imperial-guards"
compatibility_date = "2026-07-13"

# This repository is already a deployable static site. Cloudflare Pages can
# publish the repository root directly, so no build command or dist folder is required.
pages_build_output_dir = "."
