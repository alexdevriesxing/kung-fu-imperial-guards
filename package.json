{
  "name": "kung-fu-imperial-guards",
  "version": "1.0.0",
  "private": true,
  "description": "Original cinematic HTML5 martial-arts platform-fighter.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "wrangler": "^4.0.0"
  }
}
